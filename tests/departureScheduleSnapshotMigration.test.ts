import {readFileSync} from 'node:fs';import {join} from 'node:path';import {describe,expect,it} from 'vitest';const sql=readFileSync(join(process.cwd(),'supabase','migrations','202609090093_departure_schedule_and_order_snapshot.sql'),'utf8');
describe('U2 departure schedule and snapshots',()=>{it('previews and idempotently creates Japan-time batches',()=>{expect(sql).toContain("at time zone 'Asia/Tokyo'");expect(sql).toContain('departure_batch_operations');expect(sql).toContain('operation id mismatch');expect(sql).toContain('skippedDuplicates')});it('blocks capacity below committed inventory and stale edits',()=>{expect(sql).toContain('capacity below committed inventory');expect(sql).toContain('departure version conflict');expect(sql).toContain('schedule_version')});it('locks paid order price and product revision',()=>{expect(sql).toContain('order_snapshots');expect(sql).toContain('current_published_revision_id');expect(sql).toContain('on conflict(order_id) do nothing')});});
