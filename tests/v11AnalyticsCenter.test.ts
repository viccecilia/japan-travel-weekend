import{describe,expect,it}from'vitest';import{referralLevels,tokyoPeriodRange}from'../src/app/operations/AnalyticsCenter';import type{OperationsReferralRelation}from'../src/shared/integrations/supabaseOperations';
describe('V11 经营周期与推荐关系',()=>{it('周按日本时间周一至周日',()=>expect(tokyoPeriodRange('week',new Date('2026-09-13T23:30:00Z'))).toEqual({from:'2026-09-14',to:'2026-09-20'}));it('跨年季度范围稳定',()=>expect(tokyoPeriodRange('quarter',new Date('2026-12-31T16:00:00Z'))).toEqual({from:'2027-01-01',to:'2027-03-31'}));it('下游只标为分析且直接关系保持独立',()=>{const base={created_at:'',referral_code:'X',discount_percent:10,inviter_name:'',invitee_name:'',qualifying_order_id:null,order_status:null,order_amount_jpy:null,order_discount_jpy:null,trip_title:null,departs_at:null,ends_at:null,inviter_coupon_id:null,inviter_coupon_status:null,available_at:null,invitee_coupon_id:null,invitee_coupon_status:null} as unknown as OperationsReferralRelation;const rows=[{...base,id:'1',inviter_email:'a@test',invitee_email:'b@test'},{...base,id:'2',inviter_email:'b@test',invitee_email:'c@test'}];const result=referralLevels(rows);expect(result[0]).toMatchObject({direct:true,downstream:1});expect(result[1]).toMatchObject({direct:true,downstream:0})})});
