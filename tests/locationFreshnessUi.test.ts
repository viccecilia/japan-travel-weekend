import{readFileSync}from'node:fs';import{describe,expect,it}from'vitest';import{locationState}from'../src/app/operations/RunCenter';const staff=readFileSync('src/app/StaffPortal.tsx','utf8');describe('定位新鲜度显示',()=>{it('按实际采样时间区分实时与超过两分钟过期',()=>{const now=Date.parse('2026-09-12T03:00:00Z');expect(locationState('2026-09-12T02:59:00Z',now)).toContain('实时');expect(locationState('2026-09-12T02:57:59Z',now)).toContain('已过期');expect(locationState(null,now)).toBe('暂无真实定位')});it('司导采集显示最近采样时间和会话续期状态',()=>{expect(staff).toContain('最近采集');expect(staff).toContain('会话会在到期前自动续期')})});
