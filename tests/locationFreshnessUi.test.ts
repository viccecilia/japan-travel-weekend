import{readFileSync}from'node:fs';import{describe,expect,it}from'vitest';const run=readFileSync('src/app/operations/RunCenter.tsx','utf8');const staff=readFileSync('src/app/StaffPortal.tsx','utf8');describe('定位新鲜度显示',()=>{it('超过两分钟明确标记为过期',()=>{expect(run).toContain('age<=2*60_000');expect(run).toContain('已过期 · 最后定位')});it('司导采集显示最近采样时间和会话续期状态',()=>{expect(staff).toContain('最近采集');expect(staff).toContain('会话会在到期前自动续期')})});
