import{describe,expect,it}from'vitest';import{groupDeparturesByMonth,resolveDepartureSelection}from'../src/app/bookingDepartureSelection';import type{Departure}from'../src/shared/types';
const departure=(id:string,time:string):Departure=>({id,tripSlug:'route',departureTime:time,price:7000,availableSeats:10} as Departure);
describe('游客班次连续日历',()=>{it('显示完整月份、缺日以及同日多个班次',()=>{const months=groupDeparturesByMonth([departure('a','2026-12-01T00:00:00Z'),departure('b','2026-12-01T03:00:00Z'),departure('c','2026-12-31T23:30:00Z')],'zh-CN');expect(months.map(m=>m.key)).toEqual(['2026-12','2027-01']);expect(months[0].days).toHaveLength(31);expect(months[0].days[1].departures).toHaveLength(0);expect(months[0].days[0].departures.map(d=>d.id)).toEqual(['a','b']);expect(months[1].days).toHaveLength(31)});it('远期深链只选择真实存在的班次，无效参数不回退冒充',()=>{const departures=[departure('future','2027-08-01T00:00:00Z')];expect(resolveDepartureSelection({requestedId:'future',selectedId:'',departures,resolved:true})).toEqual({selectedId:'future',invalidRequested:false});expect(resolveDepartureSelection({requestedId:'missing',selectedId:'',departures,resolved:true})).toEqual({selectedId:'',invalidRequested:true})})});
