import{readFileSync}from'node:fs';import{describe,expect,it}from'vitest';const sql=readFileSync('supabase/migrations/202609100116_operations_resource_maintenance.sql','utf8');const ui=readFileSync('src/app/OperationsDashboard.tsx','utf8');describe('司导和车辆资料维护',()=>{it('更新动作受运营权限保护且写入审计',()=>{expect((sql.match(/public\.is_operations\(\)/g)??[]).length).toBe(2);expect(sql).toContain('driver_resource_updated');expect(sql).toContain('fleet_vehicle_updated')});it('车检车辆不能误设为可用',()=>{expect(sql).toContain("p_status='available' and coalesce(p_inspection_required,false)")});it('工作台提供真实保存动作',()=>{expect(ui).toContain('编辑司导资料');expect(ui).toContain('编辑车辆资料');expect(ui).toContain('updateDriverResource');expect(ui).toContain('updateFleetVehicle')})});
