import {readFileSync} from 'node:fs';import {describe,expect,it} from 'vitest';const sql=readFileSync('supabase/migrations/202608250015_notification_outbox.sql','utf8');
describe('015 履约通知 outbox',()=>{it('事件 ID 幂等且默认 pending，不伪装供应商已发送',()=>{expect(sql).toMatch(/event_id text not null unique/);expect(sql).toMatch(/default 'pending'/);expect(sql).toMatch(/on conflict\(event_id\)/)});it('必要履约通知只向订单本人和运营投影最小状态',()=>{expect(sql).toMatch(/recipient_id=auth\.uid\(\) or public\.is_operations\(\)/);expect(sql).toMatch(/grant select\(id,event_type,order_id,necessary,status,created_at,updated_at\)/);expect(sql).not.toMatch(/grant all on public\.notification_outbox to authenticated/)});it('写入与状态更新仅可信服务可执行',()=>{expect(sql).toMatch(/trusted service only/);expect(sql).toMatch(/to service_role/);expect(sql).not.toMatch(/grant execute[\s\S]*to authenticated/)})});
