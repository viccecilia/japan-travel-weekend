import {readFileSync} from 'node:fs';import {join} from 'node:path';import {describe,expect,it} from 'vitest';
const migration=(name:string)=>readFileSync(join(process.cwd(),'supabase','migrations',name),'utf8');
describe('U3 and U4 operational controls',()=>{it('uses source tables for a non-duplicating daily run board and persistent incidents',()=>{const sql=migration('202609090094_operations_run_control.sql');expect(sql).toContain('operational_incidents');expect(sql).toContain('get_operations_daily_run_board');expect(sql).toContain('left join lateral');expect(sql).toContain('open_dedup')});it('stores ordered real location points and rejects stale or out-of-order samples',()=>{const sql=migration('202609090095_driver_location_history.sql');expect(sql).toContain('driver_location_points');expect(sql).toContain('stale location');expect(sql).toContain('out of order location');expect(sql).toContain("status='completed'");expect(sql).not.toContain('simulat')});it('separates merchandising windows from sellable departures',()=>{const sql=migration('202609090096_product_merchandising.sql');expect(sql).toContain('featured_rank');expect(sql).toContain('travel_from');expect(sql).toContain('locale_readiness');expect(sql).toContain('merchandising version conflict')});});
