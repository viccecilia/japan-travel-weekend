import {Link} from 'react-router-dom';import type {Trip} from '../types';
export function TripCard({trip,app=false}:{trip:Trip;app?:boolean}){return <article className="trip-card"><img src={trip.heroImage} alt={`${trip.shortTitle} route scenery`}/><div><div className="eyebrow">{trip.region} · {trip.duration}</div><h3>{trip.title}</h3><p>{trip.summary}</p><div className="chips">{trip.categories.map(c=><span key={c}>{c}</span>)}<span>Price: TBD</span><span>Dates: Preview</span></div><Link className="text-link" to={`${app?'/app-demo/trips':'/trips'}/${trip.slug}`}>View details →</Link></div></article>}
