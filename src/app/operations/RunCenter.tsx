import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import {useApp} from '../store';import type {OperationsRunRow} from '../../shared/integrations/supabaseOperations';
const tokyoToday=()=>new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Tokyo',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());
export function RunCenter(){const {services}=useApp();const [date,setDate]=useState(tokyoToday());const [rows,setRows]=useState<OperationsRunRow[]>([]);const [notice,setNotice]=useState('');useEffect(()=>{let active=true;void services?.operations.loadRunBoard(date).then(result=>{if(active){setRows(result.data);setNotice(result.error??'')}});return()=>{active=false}},[date,services]);return <main className="operations-page"><header className="operations-hero"><div><span>DAILY OPERATIONS</span><h1>每日运行台</h1><p>来自订单、配车、点名、行程事件和异常工单；没有定位时不生成模拟轨迹。</p></div><Link className="button secondary" to="/app/operations">返回工作台</Link></header><section className="operations-section"><label>服务日期（日本时间）<input type="date" value={date} onChange={event=>setDate(event.target.value)}/></label>{notice&&<p role="alert">{notice}</p>}<div className="operations-dispatch-list">{rows.map(row=><article key={`${row.departureId}-${row.vehicleGroupId??'unassigned'}`}><div><b>{row.tripTitle}</b><span>{new Date(row.departsAt).toLocaleTimeString('zh-CN',{timeZone:'Asia/Tokyo',hour:'2-digit',minute:'2-digit'})} · {row.vehicleLabel??'未配车'} · {row.driverName??'未分配司机'}</span></div><strong>{row.bookedSeats}/{row.capacity??'—'} 人 · 已到 {row.arrived} · 已上车 {row.boarded}</strong><small>{row.journeyStatus}{row.currentStop?` · ${row.currentStop}`:''}{row.openIncidents>0?` · ${row.openIncidents} 个未关闭异常`:''}</small></article>)}</div>{!notice&&rows.length===0&&<p>该日期暂无班次。</p>}</section></main>}
