# V8-S 司导端五 Tab 整改记录（2026-09-11）

状态口径：代码完成、组件/隔离测试、预览浏览器检查、真实登录联调分别记录。预览数据截图只证明布局和交互，不作为真实账号或三端业务验收。

| 模块 | 修改内容 | 实际结果 | 证据与未验证项 |
|---|---|---|---|
| 今日 | 运行中优先，其次东京当天最近待执行；无当天任务显示下一次出勤；增加当前任务、下一步、进度、快捷入口 | 选择算法单元测试通过；预览浏览器布局通过 | `tests/v8StaffPrimaryTask.test.ts`；`output/playwright/v8s-staff-today-*-viewport.png`；真实登录待验 |
| 行程 | 今天/明天/本周/日历与状态筛选写入 URL；增加取消状态；取消任务关闭执行入口 | 组件回归通过，刷新条件由 URL 恢复 | `tests/staffPortal.test.tsx`、`/staff/schedule?date=...&status=...`；后台改期待真实环境验证 |
| 地图 | 本人任务选择、真实坐标嵌入地图、外部导航；无坐标明确空状态且不造假 | 预览验证无坐标提示和 Tab 高亮 | `output/playwright/v8s-staff-map-mobile.png`；真实地图和位置新鲜度联调待验 |
| 消息 | 行程群与工作通知分区；群状态转为中文；通知读取复用本人通知服务 | 组件回归通过 | `loadOwnNotifications` 受现有 RLS 限制；通知确认 RPC 尚无，未标记确认闭环通过 |
| 我的 | 本人称呼保存、请假、固定链接、二维码下载、收益、每周提现申请 | 代码复用现有 RPC，类型检查通过 | 资料/请假/提现的真实登录与数据库执行待验；最近7天新人明细接口尚缺 |
| 导航与视觉 | 五 Tab 图标/文字、详情页高亮、44px 触控、桌面宽屏布局、底部安全留白 | 390×844 与 1366×900 预览检查；控制台 0 error | 截图位于 `output/playwright/v8s-staff-*.png` |

## 自动验证

- `npm test`：146 个测试文件、630 项测试通过。
- `npm run typecheck`：通过。
- `npm run build`：通过，PWA precache 与 Service Worker 成功生成。
- Playwright 预览：五 Tab 可切换，深链和选中态可见，控制台 0 error。

## 明确未完成/未实测

- A/B/C 三种隔离环境真实登录司导身份尚未准备，因此无任务、待出发、运行中截图目前是组件测试或明确标记的预览证据。
- 后台派单/改期/集合点变更、司导确认、游客同步的三端真实链路未在本轮数据库执行。
- 工作通知“已读”和“已确认”分离缺少现有工作人员通知确认 RPC；当前只读展示，不伪造确认结果。
- 最近 7 天新人列表、注册/付款/完成分层统计缺少现有本人可读接口。
- 佣金调整/追偿来源订单明细尚未接入本页；核心金额和提现继续使用 V8 既有 RPC。
- 未触发真实通知、付款、退款或佣金转账，未部署生产环境。
