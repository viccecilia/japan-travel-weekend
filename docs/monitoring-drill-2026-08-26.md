# 测试 API 监控状态机演练（2026-08-26）

监控脚本同时检查 `/health` 和 dependency-aware `/ready`，只允许访问批准的 HTTPS 测试 API。状态以原子临时文件替换方式持久化；连续三次失败才打开事件，持续失败不重复告警，首次恢复产生 recovery 并清零。输出不包含请求头、会话、乘客、支付载荷或秘密。

合成演练结果依次为：失败 1（none）、失败 2（none）、失败 3（alert）、失败 4（none）、恢复（recovery）。随后真实探测返回 healthy。单元测试覆盖阈值、去重和恢复，临时状态文件已删除。

当前 `delivery` 明确为 `not_configured`，因此只能证明检测与恢复状态机，不能证明外部通知到达和值班确认。未批准告警渠道前不发送邮件、短信或即时通讯，也不建立付费账户。正式关闭门禁仍需：选择告警渠道、配置测试凭证、运行真实送达／确认／自动关闭演练并记录四个时间点。

## 隔离备份恢复前置检查

Supabase Free 组织当前已有 2 个活跃免费项目，控制台明确显示管理员已达到 `Limit: 2 free projects`，第三个项目创建按钮禁用并提示暂停、删除或升级。检查过程中没有创建、暂停或删除项目，也没有进入 Pro 或绑定付款方式。现有第二项目用途不明，禁止把它当作恢复目标覆盖。因此 `backup_restore_drill` 保持待办，直到用户指定可清空的隔离项目或批准新的免费／付费恢复环境。
