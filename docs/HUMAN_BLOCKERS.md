# Blocker Closure — 需要人工的信息

> 2026-09-20：代码已推送、测试库四个迁移已安装并复验。前端已上传，等待部署窗口 sudo 密码完成切换；详见 BLOCKER_RELEASE_20260920.md。下文未部署描述是 9/19 的历史边界，账号和西语阻断仍然有效。

日期：2026-09-19。下面不是要求重新执行 Round 0–5。代码、本地门禁和隔离数据库回滚验证已完成；尚未安装迁移或部署本轮代码，不能据此宣称线上权限修复生效。

## H1：同一批测试账号的当前登录凭据

**已定位，而非继续猜密码：** 前端、服务器本地配置和 restore 测试库指向同一项目 `hzxoofvodpqpdomtmzlf`。原内部账号文件仍保存 9/14 的旧邮箱；按账号 ID 查询，P01、D01、A01 都存在、邮箱已验证、未封禁、未删除，但邮箱已改变。旧邮箱在目标库不存在，故该文件不能再用于登录。

| 代码 | 账号 ID（必须复用） | 当前邮箱脱敏 | auth.users 最后更新时间（UTC，不代表可证实的邮箱变更时间） |
|---|---|---|---|
| P01 | c44941d4-22e4-4e6f-8d37-3c4bcda2ed5a | p01***@jtw.test | 2026-09-16 10:20:48 |
| D01 | f685b60e-c736-450b-b733-b688a3b91258 | d01***@jtw.test | 2026-09-16 10:20:47 |
| A01 | 02431f41-334c-4d01-ba2c-6521f77a8e31 | a01***@jtw.test | 2026-09-18 02:16:36 |

**最小人工输入：** 将这三个既有 ID 的当前邮箱和密码更新到原安全凭据文件，或在三个独立浏览器上下文中人工登录。不需要生产账号、service-role 密钥或生产数据库密码；不要在报告或聊天中公开密码。

本轮只读核查账号，没有再试登录，没有重置密码、创建替代账号或关闭鉴权。数据库测试在事务内切换既有身份 claims 是 RPC/RLS 测试，不是替代真实浏览器登录。恢复后仅补本轮受影响的真实身份验收，不重新跑 Round 0–5 设计工作。

## H2：西班牙语自动翻译未接通

Staff Chat 保留八语选择及原文回退。现有服务端翻译目标为简体、繁体、英语、日语、韩语、越南语、尼泊尔语；西语不在已配置目标内，本轮不接入新付费服务。

其余七语的上下文读取、缓存写入和权限已经在数据库真实执行并回滚；测试文本由测试接收器提供，不冒充外部机器翻译结果。外部翻译提供商的实际响应、质量和费用未在本轮验证。

人工可选择暂时接受西语仅原文/已有缓存的边界；若后续要求自动西语翻译，需要确认现有服务能否支持及授权范围，而非自行购买新服务。

## 后续验证边界（不是本轮新增开发）

- 未推送、未部署，四个迁移仅在回滚事务内验证。测试站安装与浏览器复测是下一次发布动作；当前运行站仍不能引用本报告称已修复。
- 登录成功后补 P01 Boost 同意/拒绝、D01 当前派班职责、A01 授权、停用/撤销后的真实页面反馈。组合职责需运营明确调用受控授权接口；本轮没有扩展管理界面或自动把现有司机改为兼任。
- 原报告中的实际支付、外部通知、Google Places 本地域名、实时 AI、旧 PWA 和三端全流程结论仍为待验证/未接通，不因本轮门禁通过而改成完成。不要求提供生产凭据来补这些测试。
- 不反复尝试旧账号、不重复调用收费翻译，不触发真实支付、退款、佣金转账或外部群发。
