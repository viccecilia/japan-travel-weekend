# V8 / V8-S / V8-S.1 统一收尾清单（2026-09-11）

验收口径：代码存在不等于隔离数据库通过；HTTP 200 不等于真实身份业务通过；未执行的项目保持未验证。

| 批次 | 问题 | 修改/提交 | 实际测试 | 结果 | 未验证 |
| --- | --- | --- | --- | --- | --- |
| A | 首页另有一套 active 选择逻辑 | `staffTaskSelection.ts`、`StaffPortal.tsx` / `68230c4` | StaffPortal 组件覆盖完成+待执行、取消+待执行、运行中+未来、仅历史、仅未来 | 代码完成；组件通过 | 已打开页面运行中被后台取消的真实 Realtime/轮询体验 |
| A | 班次取消、派单撤销未映射到司导取消状态 | 迁移 122 / `68230c4` | 迁移结构负向测试；完整回归 646 项 | 代码完成；静态负向测试通过 | 可丢弃 Supabase 项目实际执行、RPC 直接调用拒绝 |
| A | 取消后旧页面仍可执行 | 写入触发器覆盖执行事件、核销、旅程、位置；取消后关闭群、停止定位、取消集合 | `v8s1CancelledExecutionMigration.test.ts` | 代码完成 | 真实数据库旧会话与并发请求 |
| B | 前端/API 版本不可辨识 | 构建 SHA、API health/ready version、系统设置版本卡 / `48de2aa` | 类型检查、构建、专项测试 | 代码完成；构建通过 | 部署后的前后端 SHA 对照 |
| B | PWA 自动抢占并强制刷新 | prompt 更新、waiting worker、不再 controllerchange 自动 reload、API fallback denylist / `48de2aa` | `v8s1PwaVersion.test.ts`、生成 SW | 隔离构建通过 | 旧缓存浏览器与已安装旧 PWA 真机升级 |
| C | “本周”错误使用滚动 7 天 | 日本时间周一 00:00 至下周一 00:00 / `44bf964` | 周日、周一、跨月、跨年 4 项 | 通过 | 测试站真实筛选 |
| C | 无任务联系调度显示无效入口 | 有任务写入可审计 support 请求；无任务且未配置通用渠道时明确说明 / `44bf964` | StaffPortal 组件测试 | 部分完成 | 后台通用调度电话/会话配置与无任务联系渠道 |
| C | 工作通知全部进入无筛选列表 | 按 vehicleGroupId 进入具体任务；取消/变更进入带状态筛选列表；显示送达不等于确认 | `staffPortal.test.tsx` 与类型检查 | 部分代码完成 | 专用工作人员通知、已读 RPC、业务确认记录及后台回显 |
| C | 地图能力边界 | 继续只展示数据库集合点坐标；无坐标不伪造 | 既有组件测试 | 部分完成 | 当前站/下一站、真实定位更新时间、地图失败真机验收 |
| D | 三种司导真实身份、游客与运营独立会话联调 | 未执行 | 当前 CLI 未连接可丢弃 Supabase，且本机无 Docker | 未验证 | A/B/C 三类司导、三端八条流程、旧 PWA 重验 |
| D | 推荐明细、佣金追偿、争议冻结、资料维护、后台下钻 | 沿用 V8 实现与原未完成状态 | 本轮未新增完成声明 | 未完成/未验证 | 真实数据库资金业务与后台操作闭环 |

## 本轮自动验证

- `npm test -- --run`：149 个文件、646 项测试全部通过。
- `npm run typecheck`：通过。
- `npm run build`：通过；生成 prompt 模式 `sw.js` 与 Workbox 资源。
- `npx supabase status`：未通过，原因是本机没有 Docker/Podman，且当前目录没有已连接的可丢弃项目。该结果是环境阻断证据，不视为数据库通过。
- `npm run lint`：仓库现有 `output/` 构建产物与既有源码产生大量历史错误，不能作为本轮通过项；本轮使用类型检查、专项测试及全量 Vitest 回归作为代码门禁。

## 部署与回滚

本文件生成时尚未部署 V8-S.1。测试站仍为 V8-S `8adada7442fb15b6d4fb86466034bb1ca04509ee`。部署必须在迁移 122 获得可丢弃测试数据库实跑证据后执行，记录前端 SHA、API SHA、数据库迁移号和上一发布目录。
